services:
  - type: web
    name: blockchain-evidence-backend
    runtime: node
    rootDir: backend
    buildCommand: cd .. && npm install && npx hardhat compile && cd backend && npm install
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
